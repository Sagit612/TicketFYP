import { CustomerError } from "./customer-error";
export declare class BadRequestError extends CustomerError {
    message: string;
    statusCode: number;
    constructor(message: string);
    serializeErrors(): {
        message: string;
    }[];
}
