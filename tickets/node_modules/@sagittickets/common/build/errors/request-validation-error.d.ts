import { ValidationError } from "express-validator";
import { CustomerError } from "./customer-error";
export declare class RequestValidationError extends CustomerError {
    errors: ValidationError[];
    statusCode: number;
    constructor(errors: ValidationError[]);
    serializeErrors(): ({
        message: any;
        field: string;
    } | {
        message: any;
        field?: undefined;
    })[];
}
